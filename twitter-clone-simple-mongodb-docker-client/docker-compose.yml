version: '3.8'
services:
  api:
    build: .
    container_name: twitter-clone-simple-mongodb-client-frontend
    ports:
      - '2020:3000'
    volumes:
      - /app
      - /app/node_modules
    stdin_open: true
    tty: true